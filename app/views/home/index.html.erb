<header>
  <%= render "top_nav" %>

  <div class="row home-section header-intro">
    <div class="small-12 columns">
      <h3 class="serif all-caps">The adventures of</h3>
      <h1>Robin Boutros.</h1>

      <div class="row">
        <div class="small-2 columns">
          <div class="separator"></div>
        </div>
        <div class="small-6 columns end">
          <p>Rails developer building websites for a living by day, and designing them for fun by night.</p>
        </div>
      </div>
    </div>
  </div>
</header>

<%= render "home/about" %>

<%= render "home/projects" %>

<%= render "home/contact" %>

<% if false %>
<%= render "categories/category_list", categories: Category.all, legend: true %>
<% end %>
